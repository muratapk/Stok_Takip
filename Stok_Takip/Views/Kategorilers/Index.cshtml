@model IEnumerable<Stok_Takip.Models.Kategoriler>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kategori_Ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aciklama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kategori_Ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aciklama)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Kategori_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Kategori_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Kategori_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
