@model IEnumerable<Stok_Takip.Models.Satislar>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tarih)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Toplam_Tutar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Musteriler)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tarih)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Toplam_Tutar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Musteriler.Musteri_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Satislar_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Satislar_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Satislar_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
